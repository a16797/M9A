{
    "name": "m9a",
    "version": "0.0.0",
    "license": "AGPL-3.0-or-later",
    "private": true,
    "type": "module",
    "packageManager": "pnpm@11.9.0",
    "engines": {
        "node": ">=24"
    },
    "scripts": {
        "format": "pnpm format:all",
        "format:check": "prettier --check \"**/*.json\"",
        "format:json": "prettier --write \"**/*.json\"",
        "format:py": "uv run --frozen black .",
        "format:all": "pnpm format:json && pnpm format:py",
        "lint": "pnpm lint:py",
        "lint:py": "uv run --frozen ruff check .",
        "typecheck": "uv run --frozen pyright",
        "typecheck:py": "pnpm typecheck",
        "test": "uv run --frozen pytest",
        "check:py": "pnpm lint:py && pnpm typecheck:py && pnpm test",
        "check:schema": "node tools/validate-schema.mjs",
        "check:maa": "maa-tools check",
        "check": "pnpm check:py && pnpm check:schema && pnpm check:maa",
        "install:py": "uv sync --frozen --group dev"
    },
    "devDependencies": {
        "@nekosu/maa-tools": "1.0.24",
        "@nekosu/prettier-plugin-maafw-sort": "^1.0.5",
        "prettier": "3.5.3",
        "prettier-plugin-multiline-arrays": "4.1.4",
        "pyright": "^1.1.400"
    }
}
